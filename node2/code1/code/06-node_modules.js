const foo = require('foo.js');

// 如果在导入文件的时候不写路径那么他默认是到node_modules去加载


// 不跟系统的模块命名一样
// 先读取package.json文件下面去去配置项的Main
// 如果没有package.json会加载一个叫index.js, 如果都不存在就报错
const bar = require('bar'); 
console.log(module.paths);